server:
  port: ${SERVER_PORT:8080}

order:
  service-url: ${ORDER_SERVICE_URL:http://localhost:8081}  # compose와 동일 변수명

management:
  endpoints.web.exposure.include: health,info

logging:
  level:
    com.workshop.gateway: INFO
